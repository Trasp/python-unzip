from setuptools import setup

setup(name='unzip',
      version='0.1',
      description='Zip function in reverse.',
      url='http://github.com/Trasp/python-unzip',
      author='Stefan Asplund',
      license='0BSD',
      packages=['unzip'],
      zip_safe=False)
